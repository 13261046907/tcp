server:
  port: 8077

logging:
  level:
    com.insectSituation.controller: INFO

spring:
  messages:
    basename: i18n/messages
    encoding: UTF-8
    cache-seconds: 36000
  servlet:
    multipart:
      #不限制文件上传的大小，设置为-1
      #单个文件的大小
      max-file-size: 256MB
      #单次请求的文件的总大小
      max-request-size: 1024MB
  redis:
    # 地址
    host: 101.201.119.26
    # 端口，默认为6379
    port: 60019
    # 数据库索引
    database: 0
    # 密码(如没有密码请注释掉)
    password: zD%8fwgOgEhuHgKm
    # 连接超时时间
    timeout: 10s
    # 是否开启ssl
    ssl: false

  datasource:
    name: jetlinks
    username: root
    password: 3+PloXbwcM7+JA=
    url: jdbc:mysql://101.201.119.26:30016/qinhuangdao_jetlinks?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2b8
    driver-class-name: com.mysql.cj.jdbc.Driver

knife4j:
  #是否开启Knife4j增强模式
  enable: true
  #设置密码
  basic:
    enable: true
    username: test
    password: 12313
sa-token:
  # 允许动态设置 token 有效期
  dynamic-active-timeout: true
  # 允许从 请求参数 读取 token
  is-read-body: true
  # 允许从 header 读取 token
  is-read-header: true
  # 关闭 cookie 鉴权 从根源杜绝 csrf 漏洞风险
  is-read-cookie: false
  # token前缀
  token-prefix: "Bearer"
  # token 名称（同时也是 cookie 名称）
  token-name: Authorization
  # token 有效期（单位：秒） 默认30天，-1 代表永久有效
  timeout: 2592000
  # token 最低活跃频率（单位：秒），如果 token 超过此时间没有访问系统就会被冻结，默认-1 代表不限制，永不冻结
  active-timeout: -1
  # 是否允许同一账号多地同时登录 （为 true 时允许一起登录, 为 false 时新登录挤掉旧登录）
  is-concurrent: true
  # 在多人登录同一账号时，是否共用一个 token （为 true 时所有登录共用一个 token, 为 false 时每次登录新建一个 token）
  is-share: true
  # jwt秘钥
  jwt-secret-key: Dc!VaN1Il9n^
  # 是否输出操作日志
  is-log: true

# security配置
security:
  # 排除路径
  excludes:
    # 静态资源
    - /*.html
    - /**/*.html
    - /**/*.css
    - /**/*.js
    # 公共路径
    - /favicon.ico
    - /error
    # swagger 文档配置
    - /*/api-docs
    - /*/api-docs/**
    # actuator 监控配置
    - /actuator
    - /actuator/**